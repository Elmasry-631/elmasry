# تحليل شامل لموديول `meno` (Odoo 19)

---

## ⛔️ تحذير أمني خطير

**هذا الموديول هو موديول كسر/تلاعب بترخيص أودو (Crack Module).** يحتوي على كود يُستخدم لتجاوز نظام الترخيص الخاص بأودو وإطالة فترة صلاحية قاعدة البيانات بشكل غير قانوني.

**لا يُنصح بتثبيته أو استخدامه في بيئات الإنتاج.**

---

## 📌 اسم الموديول ووصفه

**الاسم:** meno

**الوصف الرسمي:** "Crack odoo v19"

**الوظيفة:** تغيير تاريخ انتهاء صلاحية قاعدة البيانات في نظام إعدادات أودو إلى تاريخ بعيد جداً (عام 5000) حتى لا تنتهي صلاحية النسخة.

---

## 📦 الإصدار والتبعيات

| العنصر | القيمة |
|---|---|
| **الإصدار** | 18.0 (خطأ — يجب أن يكون 19.0) |
| **التطبيق الرئيسي** | نعم |
| **تثبيت تلقائي** | نعم (`auto_install: True`) ⛔️ خطير |
| **يعتمد على** | `base`, `base_setup` |

> ⚠️ `auto_install: True` يعني أن هذا الموديول سيُثبت تلقائياً عند تثبيت أي موديول يعتمد على `base_setup`.

---

## 🏗️ الموديلات (Models)

### 1️⃣ `ir.module.module` (وراثة)

#### الطرق:

| الطريقة | الوصف |
|---|---|
| `crack()` | `@api.model` — يبحث عن `database.expiration_date` ويُحدّثه إلى سنة 5000 |
| `cracks()` | مشابهة لـ `crack()` — استخدام `timedelta(days=365*3000)` |
| `button_immediate_upgrade()` | **تجاوز خطير!** — كل ترقية لأي موديول ستقوم بكسر الترخيص تلقائياً |

### 2️⃣ `IrConfigParameter` (وراثة)

- نفس منطق `cracks()` ولكن على مستوى `ir.config_parameter`

---

## ⏰ البيانات (Data)

### مجدول مهام (Cron Job):
- **الاسم:** "Cracks fast scheduler"
- **الفاصل:** كل **دقيقة واحدة**
- **التشغيل الأول:** بعد 5 دقائق
- **الكود:** `model.cracks()`
- ⛔️ الكسر يحدث **تلقائياً كل دقيقة!**

---

## ⚠️ المخاطر الأمنية

| المخاطر | التفصيل |
|---|---|
| ⛔️ **غير قانوني** | كسر ترخيص البرمجيات يُعد غير قانوني في معظم الدول |
| ⛔️ **auto_install** | يُثبت تلقائياً بدون علم المستخدم |
| ⛔️ **button_immediate_upgrade** | كل ترقية موديول = كسر ترخيص |
| ⛔️ **Cron Job** | يكسر الترخيص كل دقيقة |
| ⛔️ **env.cr.commit()** | غير آمن — قد يسبب مشاكل في سلامة البيانات |
| ⛔️ **sudo()** | يتحاوز كل الصلاحيات |
| ⚠️ **كنترولر معلّق** | كان يحتوي على واجهة عامة `/crack/crack/` — يمكن لأي شخص الوصول |
| ⚠️ **طرق مكررة** | `crack()` و `cracks()` متطابقتان تقريباً |
| ⚠️ **واجهات معلّقة** | كل الواجهات والقوالب معلّقة (commented out) |
