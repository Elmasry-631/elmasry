<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_account_patch_number_list" model="ir.ui.view">
        <field name="name">account.patch.number.list</field>
        <field name="model">account.patch.number</field>
        <field name="arch" type="xml">
            <list string="Patch Numbers">
                <field name="name"/>
                <field name="description" optional="show"/>
                <field name="status" decoration-success="status == 'confirmed'" decoration-info="status == 'draft'" decoration-muted="status == 'closed'" widget="badge"/>
                <field name="company_id" groups="base.group_multi_company" optional="show"/>
                <field name="active" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_account_patch_number_form" model="ir.ui.view">
        <field name="name">account.patch.number.form</field>
        <field name="model">account.patch.number</field>
        <field name="arch" type="xml">
            <form string="Patch Number">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="status" widget="radio"/>
                            <field name="active"/>
                        </group>
                        <group>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <field name="description" placeholder="Description..."/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_account_patch_number_search" model="ir.ui.view">
        <field name="name">account.patch.number.search</field>
        <field name="model">account.patch.number</field>
        <field name="arch" type="xml">
            <search string="Patch Numbers">
                <field name="name"/>
                <field name="description"/>
                <filter string="Draft" name="draft" domain="[('status', '=', 'draft')]"/>
                <filter string="Confirmed" name="confirmed" domain="[('status', '=', 'confirmed')]"/>
                <filter string="Closed" name="closed" domain="[('status', '=', 'closed')]"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <filter string="Status" name="status" context="{'group_by': 'status'}"/>
                <filter string="Company" name="company" context="{'group_by': 'company_id'}" groups="base.group_multi_company"/>
            </search>
        </field>
    </record>

    <record id="action_account_patch_number" model="ir.actions.act_window">
        <field name="name">Patch Numbers</field>
        <field name="res_model">account.patch.number</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_account_patch_number_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Patch Number
            </p>
        </field>
    </record>
</odoo>
