<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_create_invoice_tracking"
                        string="Create Tracking"
                        type="object"
                        class="oe_highlight"
                        invisible="state != 'purchase' or tracking_count &gt; 0 "/>
            </xpath>
        </field>
    </record>


    <record id="view_purchase_order_form_inherit_btn_box" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.btn.box</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_invoice_tracking"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-list-alt"
                        invisible="tracking_count == 0">
                    <field name="tracking_count" widget="statinfo" string="Tracking"/>
                </button>
            </xpath>
        </field>

    </record>

</odoo>